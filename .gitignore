# Secretos / config local
.env
*.json
!config.example.json

# Modelo Vosk (pesado, se descarga aparte)
vosk-model-small-es-0.42/
vosk-model-small-es-0.42.zip

# Generados en runtime: fotos, audio TTS y logs diarios
logs/
saludo.mp3
saludo.hash

__pycache__/
*.pyc

# Scripts con credenciales viejas hardcodeadas (no usados, se conservan localmente sin commitear)
ocr_cloud.py
vision_mix_cloud.py

# Scripts descartados de versiones anteriores + backups temporales
old/

# Backups temporales con timestamp (ej. config.html.bak-rescan-20260806191556),
# dejados junto al archivo real antes de cada despliegue -- nunca deben commitearse
*.bak-*
